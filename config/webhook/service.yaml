
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: service
    app.kubernetes.io/instance: my-project-name-service
    app.kubernetes.io/component: webhook
    app.kubernetes.io/created-by: k8s-webhook-base
    app.kubernetes.io/part-of: k8s-webhook-base
    app.kubernetes.io/managed-by: kustomize
  name: my-project-name-service
  namespace: system
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    control-plane: my-project-name
